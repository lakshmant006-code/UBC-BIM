import { Portfolio } from '../../ui_kits/website/Portfolio.jsx';

export const metadata = {
  title: '3D Project Lab | UBC BIM',
  description: 'Rotate a project, read its spec, ask for a quote — real wood and light-gauge-steel projects, each with a live 3D model and the files we delivered.'
};

export default function ProjectsPage() {
  return <Portfolio />;
}
