import { Blogs } from '../../ui_kits/website/Blogs.jsx';

export const metadata = {
  title: 'Blog | UBC BIM',
  description: 'Updates, technical write-ups and behind-the-scenes from the UBC BIM team.'
};

export default function BlogsPage() {
  return <Blogs />;
}
