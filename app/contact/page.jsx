import { Contact } from '../../ui_kits/website/Contact.jsx';

export const metadata = {
  title: 'Contact | UBC BIM',
  description: 'Ring the bell and pick how you want to reach us — live chat, a 15-minute call, email or WhatsApp. Every route lands in our CRM.'
};

export default function ContactPage() {
  return <Contact />;
}
