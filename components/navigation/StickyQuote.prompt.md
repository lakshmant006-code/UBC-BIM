The sticky "Request a quote" button — required on every page by the brief, and the only other fixed element besides the header.

```jsx
<StickyQuote onQuote={openQuote} onChat={openChat} />
```

Bottom-right on desktop; becomes a full-width bottom bar on mobile. Do not add further floating elements.
