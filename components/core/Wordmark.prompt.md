The brand lockup. **No logo file was provided**, so this sets "UBC BIM" in Space Grotesk; pass `src` once a real file lands in `assets/`.

```jsx
<Wordmark size={22} />
<Wordmark size={20} tone="inverse" />
<Wordmark size={20} boxed />
```

Never draw, reconstruct or approximate the real UBC BIM mark.
