The capability matrix from the brief — hairline-ruled rows, banded alternates, no vertical rules.

```jsx
<CapabilityMatrix
  columns={['Machine / software', 'Type', 'File output']}
  rows={[['Revit', 'Software', 'RVT · IFC · DWG'], ['Vertex BD', 'Software', 'CSV · shop drawings']]} />
```

Populate only with machines and software UBC BIM actually supports — never guess a vendor list.
